"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// 1. Função de multiplicação
// Recebe dois números e retorna o resultado da multiplicação (também um número)
function multiplicar(a, b) {
    return a * b;
}
// 2. Função de saudação
// Recebe uma string (nome) e retorna a saudação concatenada
function saudar(nome) {
    return "Olá " + nome;
}
// Exemplos para você testar no console (opcional)
console.log(multiplicar(5, 4)); // Deve exibir 20
console.log(saudar("Lucas")); // Deve exibir "Olá Lucas"
